import styled from "styled-components";
import {Col} from "../../../atoms/styled/layout/layoutComponents";

export const TableRowCol = styled(Col)`
  filter: drop-shadow(1px 2px 10px rgba(0, 0, 0, 0.2));
`